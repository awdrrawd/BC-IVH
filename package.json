{
  "name": "bc-ivh",
  "private": true,
  "version": "2.1.1",
  "type": "module",
  "description": "Liko - IVH (Immersive Voice Hypnosis) for Bondage Club",
  "scripts": {
    "predev": "node scripts/sync-version.mjs",
    "dev": "vite build && concurrently -k -n build,serve \"vite build --watch\" \"vite preview --port 5174\"",
    "preview": "vite preview --port 5174",
    "sync-version": "node scripts/sync-version.mjs",
    "prebuild": "node scripts/sync-version.mjs",
    "build": "vite build",
    "lint": "eslint ."
  },
  "devDependencies": {
    "@eslint/js": "^9.30.1",
    "concurrently": "^9.1.0",
    "eslint": "^9.30.1",
    "globals": "^16.3.0",
    "vite": "^7.0.4"
  }
}
